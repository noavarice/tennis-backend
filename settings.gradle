rootProject.name = 'root'
rootProject.buildFileName = rootProject.name + '.gradle'
include 'rest', 'services', 'provider', 'commons', 'commons-mapstruct'

rootProject.children.each {
  it.buildFileName = it.name + '.gradle'
}
